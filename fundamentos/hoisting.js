// Significa que o Js trabalha com um sistema de içamento das variaveis, MOVENDO-AS PARA O TOPO!.
// Não declarar a variável não causa erro, retorna como undefined.
// USANDO VAR !!

// Exemplo VAR
console.log("a = ", a);
var a = 2;
console.log("a = ", a);

// Esse processo não Ocorre com LET! Deixando mais seguro o código.
// USE LET!